---
test:
  override:
    - coverage run -m py.test --ds=settings.testing_circleci --nomigrations apps/
  post:
    - coverage html -d $CIRCLE_ARTIFACTS

machine:
  python:
    version: 3.5.1
  environment:
    DJANGO_SETTINGS_MODULE: settings
